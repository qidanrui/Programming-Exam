
String.substring(int beginIndex, int endIndex) //注意是左开右闭

-------------------------------------------------------------------------------------------------------

String.toCharArray() // Converts this string to a new character array.

-------------------------------------------------------------------------------------------------------

String.equals(anObject) // 比较两个字符串是否相同，就用equals方法，必要用==

-------------------------------------------------------------------------------------------------------

String.compareTo(another String) //返回值分大于0，小于0，等于0三种情况
