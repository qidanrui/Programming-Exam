split之前最好trim一下，以去除String之前和之后的空格

-------------------------------------------------------------------------------------------------------

String[] words = sentence.trim().split("[ ,.!?]+");// 进行分词，[]表示或者，加号表示一个或多个

-------------------------------------------------------------------------------------------------------

score = input.split("(\\s)+");// (\\s)+就可以去掉所有的空白字符，包括空格，tab，回车换行等
